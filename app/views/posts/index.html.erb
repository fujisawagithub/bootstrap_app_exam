<h1>つぶやき一覧</h1>
<%= notice %></p>

<table>
    <tr>
      <th>いまどうしてる？</th>
    </tr>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.content %></td>
        <td><%= link_to '詳細を確認する', posts_path(post.id) %></td>
        <td><%= link_to '編集する', edit_post_path(post) %></td>
        <td><%= link_to '削除する', posts_path(post.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
</table>

<br>
<%= link_to 'つぶやきの投稿', new_post_path %>
