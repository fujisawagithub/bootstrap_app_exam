<%= notice %></p>
<table>
  <tr>
    <th><h1>つぶやき一覧</h1></th>
  </tr>
    <% @posts.each do |post| %>
  <tr>
    <td><%= post.content %></td>
    <td><%= link_to '詳細を確認する', post_path(post.id) %></td>
    <td><%= link_to '編集する', edit_post_path(post) %></td>
    <td><%= link_to '削除する', post_path(post.id), method: :delete, data: { confirm: '本当にいいですか？' } %></td>
  </tr>
  <% end %>
</table>
